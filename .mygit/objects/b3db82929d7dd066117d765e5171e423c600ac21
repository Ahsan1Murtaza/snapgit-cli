#include "GetHeadRef.h"
#include <fstream>
#include <string>

std::string GetHeadRef::getRef() {
    std::ifstream headFile(".mygit/HEAD");
    std::string ref;
    if (headFile.is_open()) std::getline(headFile, ref);
    return ref;
}
