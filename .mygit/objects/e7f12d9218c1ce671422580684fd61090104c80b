#include "UpdateHead.h"
#include <fstream>

void updateHead(const std::string& newHash) {
    std::ofstream headFile(".mygit/HEAD");
    if (headFile.is_open()) {
        headFile << newHash;
        headFile.close();
    }
}
